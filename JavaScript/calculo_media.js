//Faça um algoritimo que dado as 3 notas tiradas por um aluno em um semestre
//da faculdade, calcule e imprima a sua media e a sua classificação conforme tabela.

// Media = (nota1 + nota2 + nota3) / 3

//Classificacão
//-media  menor que 5, reprovação
//-media entre 5 e 7, recuperacao
//- media acima de 7, passou de FileSystemWritableFileStream

const nota1 = 5;
const nota2 = 7;
const nota3 = 9;
const media = (nota1 + nota2 + nota3) / 3;
const resultado = media

console.log(media);

if (media <= 5) {
    console.log("Reprovado");
} else if (media >= 5 && media <= 7) {
    console.log("recuperação");
} else {
    console.log("Passou de Semestre")
}

const numero = 5;
if (numero % 2 ===)) {
    console.log('numero par');
}else {
    console.log('numero impar');
}
